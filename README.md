# Easy Ajax

How to use?

```js
const configExample = {
  ajax_url: "/ajax.php",
};
const dataExample = {
  user: inputUser.value,
  pwd: inputPwd.value,
  action: config.action,
};
easyAjax(configExample, dataExample);
```
